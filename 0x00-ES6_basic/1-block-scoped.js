export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    /*
    Commented out to avoid error
    let task = true;
    let task2 = false;
    */
  }

  return [task, task2];
}
